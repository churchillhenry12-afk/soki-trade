# MT5 bridge service

This directory is the deployment boundary for the future Windows-only bridge.
Milestone one deliberately contains no broker package and no order endpoint.
The exact API and security requirements are in `docs/MT5_BRIDGE.md`.

